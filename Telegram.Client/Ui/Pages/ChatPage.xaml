<Page x:Class="Telegram.Ui.Pages.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:chatPage="clr-namespace:Telegram.Ui.UserControls.ChatPage"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="350"
      Loaded="OnLoaded">
    <Grid Background="{StaticResource ChatBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <chatPage:Header/>
        <chatPage:Body 
            Grid.Row="1"
            Messages="{Binding Chat.Messages}"
            VerticalContentAlignment="Stretch"/>
        <chatPage:MessageInput 
            Grid.Row="2"
            SendCommand="{Binding SendMessageCommand}"
            Message="{Binding Message, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</Page>
